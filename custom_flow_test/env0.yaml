version: 2

deploy:
  steps:
    setupVariables:
      after:
        - name: Check for files
          run: |
            ls -la
            ls -la .terraform
    terraformInit:
      before:
        - name: Add a file
          run: |
            touch $FILENAME
            touch .terraform/$FILENAME
